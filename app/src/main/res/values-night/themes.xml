<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MyTodoApp" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/dark_primaryColor</item>
        <item name="colorPrimaryVariant">@color/dark_primaryDarkColor</item>
        <item name="colorOnPrimary">@color/dark_primaryTextColor</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/dark_secondaryColor</item>
        <item name="colorSecondaryVariant">@color/dark_secondaryDarkColor</item>
        <item name="colorOnSecondary">@color/dark_secondaryTextColor</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="OutlinedButton" parent="Widget.Material3.Button.OutlinedButton">
        <item name="strokeColor">@color/dark_strokeColor</item>
        <item name="android:textColor">@color/dark_primaryTextColor</item>
    </style>
</resources>